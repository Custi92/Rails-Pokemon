<div class="container">
  <ul>
  <% @trainers.each do |trainer| %>
    <li>
      <%= link_to trainer.name, trainer_path(trainer) %>
      <%= trainer.age %>
      <%= cl_image_tag trainer.photo.key %>
    </li>
    <% end %>
  </ul>
</div>
